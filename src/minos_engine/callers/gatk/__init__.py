"""GATK HaplotypeCaller — the one and only executable/selectable caller.

Stage 0 provides the canonical 25-parameter registry, CONFIG canonicalization
and validation, and CLI-flag metadata. It does NOT execute GATK.
"""
