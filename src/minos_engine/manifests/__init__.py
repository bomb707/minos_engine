"""Release manifests — reproducibility identity for the engine build.

Required identities are present and non-null; an empty git/scorer/upstream
identity fails closed (Overall spec Appendix A; assignment §14).
"""
