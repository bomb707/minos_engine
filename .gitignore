# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
coverage.xml

# Ephemeral engine runtime (never committed).
# Anchored to the repo root so it does NOT match config dirs like configs/runtime/.
/runtime/

# Transient CI output (generated by the CI workflow, not versioned)
reports/ci-*.xml

# Layer 1 generated profile artifacts (runtime output, never committed).
# The committed real-BAM *integration report* (reports/LAYER1_REAL_BAM_REPORT.json)
# is evidence and is intentionally NOT ignored.
*.parquet
bam-profile-v1.json
profile-manifest-v1.json

# Large genomic / truth artifacts — MUST NOT be committed
*.bam
*.bai
*.cram
*.vcf
*.vcf.gz
*.vcf.gz.tbi
*.fa
*.fasta
*.fai
*.dict
*.sdf/
truth.*
mutations.*

# OS
.DS_Store
